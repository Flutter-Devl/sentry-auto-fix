name: codeguardian_reporter
description: Report generation (console, JSON, HTML, SARIF, etc.) for analysis results.
version: 0.1.0
publish_to: none

environment:
  sdk: '^3.4.0'

dependencies:
  codeguardian_core:
    path: ../codeguardian_core

dev_dependencies:
  codeguardian_native:
    path: ../codeguardian_native
  codeguardian_rules:
    path: ../codeguardian_rules
  html: ^0.15.0
  json_schema: ^5.2.2
  lints: ^4.0.0
  path: ^1.9.0
  test: ^1.25.0
